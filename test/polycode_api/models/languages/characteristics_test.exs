defmodule MoipFlow.Models.Languages.CharacteristicsTest do
  use ExUnit.Case
  doctest MoipFlow.Models.Languages.Characteristics

  test "greets the world" do
    assert MoipFlow.soma(1,2) == 3
  end
end
